{% extends 'Admin/baseAdmin.html.twig' %}

{% block title %}Etablissement{% endblock %}

{% block body %}
<div class="app-wrapper">
	    
    <div class="app-content pt-3 p-md-3 p-lg-4">
        <div class="container-xl">
    <h1>Etablissement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etablissement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etablissement.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ etablissement.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ etablissement.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etablissement-index') }}">back to list</a>

    <a href="{{ path('etablissement-edit', {'id': etablissement.id}) }}">edit</a>

    {{ include('Admin/etablissement/_delete_form.html.twig') }}
    </div>
    </div>
</div>
{% endblock %}
